/*
module lobby.factories {
  "use strict";
  export class ResourceBuilder{

    public static $inject = [
      '$resource'
    ];

    constructor(private $resource: ng.resource.IResourceService) {
    }

    public getRoomResource(): lobby.interfaces.IRoomResource {
      return this.$resource('http://localhost:2999/lobby/:id', { id: '@id' }, {
      });
    }
  }
}
*/
